import 'package:flutter/material.dart';

class LiveScrorerScreen extends StatelessWidget {
  const LiveScrorerScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
